class Solution {
  public:
    string profession(int level, int pos) {
        // code here
        int flips = __builtin_popcount(pos - 1); // 0 based;
        
        return (flips & 1) ? "Doctor" : "Engineer";
    }
};
